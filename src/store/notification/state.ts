const mappings: any = {};

export default {

    //datasetList: { },


    /*

    //ping 오는대로 list로 업데이트
    sseIdx: 0,
    sseSampleList: [],

    //같은 data면 상태(Idx)만 업데이트
    sIdx: 0,
    sseSamples: [],

    */

    connectState: false,

    notificationList: [],

    /*
     * 1. 데이터 임포트
     *      (1) 원본 데이터 이름
     *      (2) 이벤트 로그 이름
     *      (3) 파일 업로드: 파일명
     *      (4) 요청일시
     * 2. 데이터 임포트
     *      (1) 원본 데이터 이름
     *      (2) 이벤트 로그 이름
     *      (3) DB 연결: DB type, host, port, DB, table
     *      (4) 요청일시
     * 3. 데이터 필터
     *      (1) 원본 데이터 이름
     *      (2) 이벤트 로그 이름
     *      (3) 요청일시
     * 4. 데이터 매핑
     *      (1) 원본 데이터 이름
     *      (2) 이벤트 로그 이름
     *      (3) 상태
     *      (4) 요청일시
     * 
     */
}