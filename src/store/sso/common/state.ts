import { ISetting } from '@/interface';
import { DefaultSetting } from '@/interface/ISetting';

const setting: ISetting = DefaultSetting;

export default {
  myInfo: null,
  projectId: null,
  setting: setting,
};
